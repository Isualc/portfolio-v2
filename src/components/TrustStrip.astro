---
interface Props { lang?: "de" | "en"; }
const { lang = "de" } = Astro.props;

const items = {
  de: ["100 % Offline", "DSGVO by Design", "Made in Austria", "Edge-AI mit Gemma 4", "Pflege-Praxis als Fundament"],
  en: ["100 % Offline", "GDPR by Design",  "Made in Austria", "Edge-AI with Gemma 4", "Nursing practice as foundation"],
}[lang];
---

<section class="trust" aria-label="Vertrauensmerkmale">
  <div class="container trust-inner">
    {items.map((item, i) => (
      <>
        <span>{item}</span>
        {i < items.length - 1 && <span class="sep" aria-hidden="true">·</span>}
      </>
    ))}
  </div>
</section>
